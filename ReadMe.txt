How to run application:
1.Go to home location ~
2.(mongoDB/bin)mongod --dbpath MongoDataDirectory command
3. make sure mongodb is installed and path is set before 2
4. run node .

To do list:
bring all constants at one place and place it in a json file
write and test /updateUser:id API